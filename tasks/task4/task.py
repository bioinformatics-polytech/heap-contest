from typing import List


def task4(forecast: List[int]) -> list:
    """
    Parameters:
    :forecast (list): list with values means next:
        0 - no rain at this day
        > 0 - rain on the lake number i
    :return: list with government actions or empty list
    """
    pass
